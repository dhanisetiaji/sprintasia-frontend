export interface Store {
  store_id: number;
  kode: string;
  nama: string;
  alamat: string;
  no_telepon: string;
  created_at: string;
  updated_at: string;
  is_active: boolean;
  is_delete: boolean;
}
